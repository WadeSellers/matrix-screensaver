name: MatrixSaver
options:
  bundleIdPrefix: com.wadesellers
  deploymentTarget:
    macOS: "14.0"
  developmentLanguage: en
  defaultConfig: Release
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: minimal
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    ENABLE_HARDENED_RUNTIME: YES
    DEAD_CODE_STRIPPING: YES
    CODE_SIGN_STYLE: Automatic
    # Wade's Apple Developer Program team. Xcode auto-fetches the
    # right Apple Development / Apple Distribution certificate based
    # on whether we're building locally or for upload.
    DEVELOPMENT_TEAM: GV76VP4G8V
    CLANG_ENABLE_MODULES: YES

targets:
  MatrixCore:
    type: framework
    platform: macOS
    sources:
      - path: MatrixCore
    settings:
      base:
        DEFINES_MODULE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.wadesellers.MatrixCore
        DYLIB_COMPATIBILITY_VERSION: 1
        DYLIB_CURRENT_VERSION: 1
        DYLIB_INSTALL_NAME_BASE: "@rpath"
        SKIP_INSTALL: YES
        BUILD_LIBRARY_FOR_DISTRIBUTION: NO
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: "0.1.0"
    info:
      path: MatrixCore/Info.plist
      properties:
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"

  SaverTest:
    type: application
    platform: macOS
    sources:
      - path: SaverTest
    dependencies:
      - target: MatrixCore
        embed: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.wadesellers.SaverTest
        LD_RUNPATH_SEARCH_PATHS: "@executable_path/../Frameworks"
        ASSETCATALOG_COMPILER_APPICON_NAME: ""
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: "0.1.0"
    info:
      path: SaverTest/Info.plist
      properties:
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        LSMinimumSystemVersion: "14.0"
        NSHighResolutionCapable: true
        NSSupportsAutomaticTermination: false
        NSSupportsSuddenTermination: false
        NSPrincipalClass: NSApplication

  # Target name stays "MatrixApp" (and folder stays MatrixApp/) for
  # internal consistency with the Swift class names — only the
  # *user-facing* identity rebrands to Cipherfall.
  MatrixApp:
    type: application
    platform: macOS
    sources:
      - path: MatrixApp
    dependencies:
      - target: MatrixCore
        embed: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.wadesellers.cipherfall
        PRODUCT_NAME: Cipherfall
        LD_RUNPATH_SEARCH_PATHS: "@executable_path/../Frameworks"
        ASSETCATALOG_COMPILER_APPICON_NAME: ""
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: "0.1.0"
        # App Sandbox — required for Mac App Store distribution.
        # See MatrixApp/Cipherfall.entitlements for the entitlement set.
        CODE_SIGN_ENTITLEMENTS: MatrixApp/Cipherfall.entitlements
    info:
      path: MatrixApp/Info.plist
      properties:
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        CFBundleIconFile: "AppIcon"
        LSMinimumSystemVersion: "14.0"
        NSHighResolutionCapable: true
        # Menu-bar app — no Dock icon, no main menu bar item except via
        # NSStatusItem.
        LSUIElement: true
        NSSupportsAutomaticTermination: false
        NSSupportsSuddenTermination: false
        NSPrincipalClass: NSApplication
        NSHumanReadableCopyright: "© 2026 Wade Sellers"
        # URL scheme: cipherfall://activate, cipherfall://dismiss,
        # cipherfall://toggle. Use to wire up Shortcuts.app, Raycast,
        # Alfred, Stream Deck, Hammerspoon, AppleScript — anything that
        # can open a URL.
        CFBundleURLTypes:
          - CFBundleURLName: com.wadesellers.cipherfall
            CFBundleURLSchemes:
              - cipherfall

  MatrixSaver:
    type: bundle
    platform: macOS
    sources:
      - path: MatrixSaver
    dependencies:
      - target: MatrixCore
        embed: true
        codeSign: true
      - sdk: ScreenSaver.framework
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.wadesellers.MatrixSaver
        PRODUCT_NAME: MatrixSaver
        WRAPPER_EXTENSION: saver
        LD_RUNPATH_SEARCH_PATHS: "@executable_path/../Frameworks @loader_path/../Frameworks"
        SKIP_INSTALL: NO
        ENABLE_HARDENED_RUNTIME: NO
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: "0.1.0"
        COMBINE_HIDPI_IMAGES: YES
    info:
      path: MatrixSaver/Info.plist
      properties:
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        CFBundlePackageType: BNDL
        NSPrincipalClass: MatrixSaverView
        LSMinimumSystemVersion: "14.0"
        NSHighResolutionCapable: true
